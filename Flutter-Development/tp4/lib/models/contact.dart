class Contact {
  int? id;
  String? name;
  String? mobile;

  // The constructor inside the curly braces {} means these parameters are optional and named.
  Contact({this.id, this.name, this.mobile});
}
